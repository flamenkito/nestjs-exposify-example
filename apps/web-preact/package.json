{
  "name": "@example/web-preact",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "workgraph": {
    "sources": {
      "web-preact": {
        "command": "exposify-codegen api --output ./src/generated --target preact",
        "deps": ["api"]
      }
    }
  },
  "dependencies": {
    "@example/utils": "*",
    "@preact/signals": "^1.3.1",
    "preact": "^10.25.4"
  },
  "devDependencies": {
    "@example/api": "*",
    "@preact/preset-vite": "^2.9.4",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  }
}
